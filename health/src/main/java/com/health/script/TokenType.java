package com.health.script;

public enum TokenType {
    UNKNOWN,
    IDENTIFIER,
    NUMBER_LITERAL,
    STRING_LITERAL,
    NULL_LITERAL,
    BOOL_LITERAL,
    KEYWORD,
    OPERATOR,
    PUNCTUATOR,
    COMMENT,
    TYPE
}
