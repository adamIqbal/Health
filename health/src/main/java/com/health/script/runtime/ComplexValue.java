package com.health.script.runtime;

/**
 * Represents a complex value in the script.
 */
public class ComplexValue extends Value {
    /**
     * Creates a new value with the given {@link ScriptType}.
     *
     * @param type
     *            the type of the value.
     */
    public ComplexValue(final ScriptType type) {
        super(type);
    }
}
