//with metingen as table0 and after that all admire txt measurement

var table = constrain table0 where Login = "admire2";

table = connect table1 with table where ModifiedDate = date as date;

var event = code table as measurement : value > 0 , entry : Value > 0;


var seq = sequence("measurement", "entry", true);
findSequences(event , seq);
table = seq.toTable();
table = constrain table where Value > 0;
table = chunk table by Login select count of Login;

seq = sequence("measurement", "measurement",  "entry", true);
findSequences(event , seq);
var tmp = seq.toTable();
tmp = constrain tmp where Value > 0;
tmp = chunk tmp by Login select count of Login;
table = connect table with tmp where date = date;

seq = sequence("measurement", "measurement", "measurement", "entry", true);
findSequences(event , seq);
tmp = seq.toTable();
tmp = constrain tmp where Value > 0;
tmp = chunk tmp by Login select count of Login;
table = connect table with tmp where date = date;

seq = sequence("measurement", "measurement", "measurement", "measurement", "entry", true);
findSequences(event , seq);
tmp = seq.toTable();
tmp = constrain tmp where Value > 0;
tmp = chunk tmp by Login select count of Login;
table = connect table with tmp where date = date;

seq = sequence("measurement", "measurement", "measurement", "measurement", "measurement", "entry", true);
findSequences(event , seq);
tmp = seq.toTable();
tmp = constrain tmp where Value > 0;
tmp = chunk tmp by Login select count of Login;
table = connect table with tmp where date = date;

seq = sequence("measurement", "measurement", "measurement", "measurement", "measurement", "measurement", "entry", true);
findSequences(event , seq);
tmp = seq.toTable();
tmp = constrain tmp where Value > 0;
tmp = chunk tmp by Login select count of Login;
table = connect table with tmp where date = date;

seq = sequence("measurement", "measurement", "measurement", "measurement", "measurement", "measurement", "measurement", "entry", true);
findSequences(event , seq);
tmp = seq.toTable();
tmp = constrain tmp where Value > 0;
tmp = chunk tmp by Login select count of Login;
table = connect table with tmp where date = date;

seq = sequence("measurement", "measurement", "measurement", "measurement", "measurement", "measurement", "measurement", "measurement", "entry", true);
findSequences(event , seq);
tmp = seq.toTable();
tmp = constrain tmp where Value > 0;
tmp = chunk tmp by Login select count of Login;
table = connect table with tmp where date = date;

write("out.txt" , table);

freqbar(table, "count_Login");